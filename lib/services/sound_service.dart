/// Stub sound service — replace with a real audio package (e.g. audioplayers)
/// when you're ready to add tap/click sounds.
class SoundService {
  SoundService._();

  /// Play a short tap sound on button presses.
  static void tap() {
    // TODO: implement with audioplayers or similar package
  }
}
